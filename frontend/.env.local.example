# Copy this file to .env.local and adjust as needed.
# .env.local is gitignored so it's safe to put dev-only values there.

# URL of the AIgriculture FastAPI backend. Set to wherever you ran
# `uvicorn aigriculture.api.app:app`. Default is the local dev port.
NEXT_PUBLIC_API_URL=http://localhost:8008
